[pytest]
testpaths = tests
addopts = -m "not legacy_windows"
markers =
    legacy_windows: retired Windows installer, executable resolution, or PyInstaller/PowerShell packaging checks; excluded from the macOS/Linux suite
